load("@rules_python//python:defs.bzl", "py_library")
load("//tests/infra:golden_test.bzl", "golden_test")

py_library(
    name = "wbb",
    srcs = glob(["**/*.py"]),
    deps = ["//src/p4py"],
)

golden_test(
    name = "wbb_golden_test",
    golden_file = "wbb.p4",
    p4_library = ":wbb",
    p4_module = "tests.e2e.sai_p4.instantiations.google.wbb",
)

# TODO: Add p4testgen test once sub-control instantiation is supported.
# The acl_wbb_ingress control is called from ingress but not emitted as a
# standalone control block. P4Testgen fails with "declaration not found".
